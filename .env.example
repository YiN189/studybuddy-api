# MongoDB Atlas (cloud) — replace with your connection string from Atlas
# Get it: Atlas → your cluster → Connect → Connect your application
# Replace <password> with your DB user password (URL-encode special chars: # → %23, @ → %40)
MONGODB_URI=mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority

# Database name (optional, default: studybuddy)
MONGODB_DB=studybuddy

# Frontend URL for CORS (optional, default: http://localhost:5173)
FRONTEND_URL=http://localhost:5173

# --- Local MongoDB (if not using Atlas) ---
# MONGODB_URI=mongodb://localhost:27017
